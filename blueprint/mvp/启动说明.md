---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 4263874b1ccbbfa992fe963c5a172284_292f0ac05cec11f19299525400d9a7a1
    ReservedCode1: P1btcDZgZUUPQoQqj9JimVP2Vjg2KAb9/e2J3SaZJN0JrA3+LD6dM7Wc8h9JEIo8fJEMRtBRr6UpPc8yPThtfAeabZyXvUiRwdctrYbTjIMZSOUYU7HaY85B1YCx4AwM1e/BB8NuP4go0yERwgULeTbdX9P1UGci3BU8LaYfxzEZk1fvVyMLjqMDjOo=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 4263874b1ccbbfa992fe963c5a172284_292f0ac05cec11f19299525400d9a7a1
    ReservedCode2: P1btcDZgZUUPQoQqj9JimVP2Vjg2KAb9/e2J3SaZJN0JrA3+LD6dM7Wc8h9JEIo8fJEMRtBRr6UpPc8yPThtfAeabZyXvUiRwdctrYbTjIMZSOUYU7HaY85B1YCx4AwM1e/BB8NuP4go0yERwgULeTbdX9P1UGci3BU8LaYfxzEZk1fvVyMLjqMDjOo=
---

# WenShanKB MVP 启动说明

> 文山州AI创作公共知识库 · MVP v0.1.0  
> 部署日期: 2026-05-31 | 状态: ✅ 运行中

---

## 1. 访问地址

| 入口 | URL | 说明 |
|------|-----|------|
| 用户端 | http://localhost:8081/ | 知识库浏览与搜索 |
| 管理后台 | http://localhost:8081/admin/ | 内容管理 SPA |
| API 文档 | http://localhost:8001/docs | Swagger UI |
| API ReDoc | http://localhost:8001/redoc | 自动生成文档 |
| 健康检查 | http://localhost:8001/api/v1/health | 服务状态 |

> 实际端口映射（容器内→宿主机）：Nginx 80→8081 / API 8000→8001 / PG 5432→5434

---

## 2. Docker Compose 管理

```bash
cd D:\WenShanKB\blueprint\mvp

# 启动
docker compose up -d

# 查看状态
docker compose ps

# 查看日志
docker compose logs -f api
docker compose logs -f db
docker compose logs -f nginx

# 重启某服务
docker compose restart api

# 停止
docker compose down
```

## 3. 默认凭证

| 用途 | 值 |
|------|-----|
| 数据库用户 | `wskb_user` |
| 数据库密码 | `wskb_secret_2025` |
| 数据库名   | `wenshankb` |
| API Key（开发） | `wskb-dev-key` |

## 4. API 测试示例

### 健康检查
```bash
curl http://localhost:8001/api/v1/health
# {"status":"healthy","version":"0.1.0","database":"connected","vector_index":"not_initialized"}
```

### 搜索笔记
```bash
curl -X POST http://localhost:8001/api/v1/search \
  -H "Content-Type: application/json" \
  -H "X-API-Key: wskb-dev-key" \
  -d '{"query": "三七", "page": 1, "page_size": 10, "search_mode": "hybrid"}'
```

### 获取提示词模板
```bash
curl "http://localhost:8001/api/v1/prompts?scenario=文旅推广" \
  -H "X-API-Key: wskb-dev-key"
```

### 合规检查
```bash
curl -X POST http://localhost:8001/api/v1/compliance/check \
  -H "Content-Type: application/json" \
  -H "X-API-Key: wskb-dev-key" \
  -d '{"content": "文山三七可以治疗...", "content_type": "农产品带货", "check_categories": ["all"]}'
```

## 5. 目录结构

```
mvp/
├── docker-compose.yml     # 三服务编排（PG + FastAPI + Nginx）
├── Dockerfile.api          # API 镜像构建
├── nginx.conf              # Nginx 反向代理配置
├── db_schema.sql           # 数据库 Schema（16 表 + pgvector）
├── app.py                  # FastAPI 应用（825 行 / 10 端点）
├── api_spec.yaml           # OpenAPI 3.0 规范
├── requirements.txt        # Python 依赖
├── 启动说明.md              # 本文件
├── admin/index.html        # 管理后台 SPA
├── public/
│   ├── index.html          # 用户端首页
│   ├── search.html         # 搜索页
│   ├── note.html           # 笔记详情页
│   └── ai-tools.html       # AI 创作工具页
└── migrations/             # 数据库迁移脚本（待填充）
```

## 6. 服务架构

```
Browser ──→ Nginx (:8081) ──→ /          → public/   (用户端)
                              /admin/    → admin/    (管理后台)
                              /api/*     → FastAPI   (后端)
                              /docs      → Swagger

FastAPI (:8001) ──→ PostgreSQL (:5434) + pgvector
```

## 7. 已知限制

- 管理后台编辑/删除/审核需完整 CRUD endpoint（当前仅读取与搜索）
- 前端在 API 不可用时自动降级为浏览器模拟数据
- JWT 认证框架已预留，待后续实现
- 未做移动端深度适配
*（内容由AI生成，仅供参考）*
